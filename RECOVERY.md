# 数据恢复指南

## 如果登录后书籍列表消失了

### 快速检查：数据是否真的丢失？

1. **打开浏览器开发者工具**（F12 或 Cmd+Option+I）
2. **进入 Application 标签页** → 左侧 Storage → IndexedDB → ZenReaderDB → books
3. **查看是否有数据**：
   - ✅ 如果能看到书籍记录 → 数据还在，只是前端状态问题
   - ❌ 如果表是空的 → 数据已被清空

---

## 场景 1：数据还在（只是前端状态问题）

**刷新页面即可恢复**，修复后的代码会在登录后自动重新加载书籍列表。

```bash
# 重新构建并启动
npm run build
npm run dev
```

---

## 场景 2：数据已丢失

### A) 从云端恢复（如果之前已同步过）

如果你在其他设备上有同样的书籍并且已经同步到云端：

1. **重新导入书籍文件**（.txt/.epub/.pdf）
2. **登录同一账号**
3. **打开书籍** → 系统会自动从云端拉取进度
4. 如果 hash 不匹配，点击书卡上的"关联进度"按钮手动绑定

### B) 从本地备份恢复（如果你导出过备份）

1. 点击书架页面右上角 **导出** → **恢复备份**
2. 选择之前导出的 `zenreader_metadata_backup_*.json` 文件
3. 系统会恢复所有书籍的元数据（**但不包括书籍文件本身**）
4. 你需要重新导入原始的书籍文件

### C) 手动重建 IndexedDB（高级）

**警告：仅在确认数据完全丢失且无备份时使用**

```javascript
// 在浏览器控制台执行
// 1. 完全删除旧数据库
indexedDB.deleteDatabase('ZenReaderDB');

// 2. 刷新页面，系统会自动重建干净的数据库

// 3. 重新导入所有书籍
```

---

## 预防措施（避免再次发生）

### 1. 定期导出备份

每周点击一次 **导出** → **备份数据**，保存 JSON 文件到安全位置（云盘/U盘）。

### 2. 启用云端同步

- 登录 GitHub/Google/Email 账号
- 系统会自动同步阅读进度到云端
- 即使本地数据丢失，也能从云端恢复进度（但书籍文件需要重新导入）

### 3. 保留原始书籍文件

将所有 .txt/.epub/.pdf 文件保存在一个文件夹中（如 `~/Books/`），方便随时重新导入。

---

## 已修复的 Bug

**问题：** 登录后书籍列表消失

**原因：** `onAuthStateChange` 回调中没有重新加载书籍列表，导致 React 状态与 IndexedDB 不同步

**修复：** 
- 登录时自动重新加载书籍列表
- 回滚数据库版本号避免不必要的升级
- 添加详细的日志便于调试

**验证：**
```bash
# 重新构建
npm run build

# 启动开发服务器
npm run dev

# 登录后在控制台查看日志
# 应该能看到：[Auth] Logged in, reloaded X books
```

---

## 联系支持

如果问题仍然存在，请：

1. 打开浏览器控制台（F12）→ Console 标签页
2. 截图所有红色错误信息
3. 检查 IndexedDB 中的数据是否存在
4. 提供以上信息以便排查
